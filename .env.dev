# Development environment variables for docker-compose.dev.yml
# Replace with values appropriate for your environment
POSTGRES_USER=dbuser
POSTGRES_PASSWORD=dbpass
POSTGRES_DB=dbcar

# Optional: override Spring profile used by the container
# SPRING_PROFILES_ACTIVE=prod
